{
  "routes": [{
      "method": "GET",
      "path": "/nas-status",
      "handler": "nas-status.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/nas-status",
      "handler": "nas-status.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/nas-status",
      "handler": "nas-status.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
